# Superseded — see `DEFECTS.md`

**Status: merged and closed, 10 August 2026.** Do not read this file for current state; it will not be updated.

This file held the Track C test log across five passes, but it was **overwritten each pass rather than appended to**, so only the last one survived here. What could be recovered — passes 1 through 5, what each fixed, and what each opened — is reconstructed in the dated sections of `DEFECTS.md`.

That overwriting is the reason `DEFECTS.md` is append-only. How many attempts a fix took is evidence about the defect, and this file destroyed it four times.

This file could not be deleted (`rm: Operation not permitted` on this folder), so it is stubbed rather than removed.
